# ESP-IDF build artifacts
apps/nc2000_tab5/build/
build/
managed_components/
sdkconfig.old
dependencies.lock

# Debug captures / logs
*.txt
!requirements.txt

# Merged flash image (distributed via GitHub Releases, not the repo)
tab5-nc2000-merged.bin

# ROM / firmware data (copyrighted, large — lives on the SD card)
assets/*.bak
assets/*.bin
*.rom
*.nor
*.nand
*.nand0
*.state

# Editor / OS
.vscode/
.DS_Store
Thumbs.db
